import NavBar from "../navbar";

export default function Header(){
    return(
        <header className="bg-bg_color text-text_color flex flex-row items-center justify-between pr-[5%] pl-[5%] ">
                <p>img</p>
                <NavBar/>
        </header>
    )
}